#include <lib/stl_algorithms.h>
#include <vector>
//#include <algorithm>
#include <list>
#include <array>


int main() {
//    std::vector<int> v = {1, 2, 3};
//    std::list<int> l = {1, 1, 3, 4};
//    std::cout << stl::all_of(l.begin(), l.end(), [](int i){ return i == 0; });
//    std::cout << is_partitioned(v.begin(), v.end(), [](std::string x) { return x == "sdf"; });
//    std::cout << *find_backward(l.begin(), l.end(), 3);
//    auto x = xrange(5);
//    for (auto i : stl::xrange(1, 10)) {
//        std::cout << i << ' ';
//    }
//    auto x = xrange(1, 5, 1);
//    int* x = new int(1);
//    *x = *x + 1;
//    std::cout << *x;
//    auto x = xrange(-1, -123, -5);
//    for (auto i : x) {
//        std::cout << i << " ";
//    }

//    auto y = xrange(10, 1, -1);
//    std::vector<int> v{y.begin(), y.end()};
//    for (auto i : v) {
//        std::cout << i << " ";
//    }

//    auto z = stl::xrange(10, 1, -1);
//    std::vector<double> w{z.begin(), z.end()};
//    for (auto i : w) {
//        std::cout << i << " "; // 1.5 2.5 3.5 4.5
//    }

//    std::vector<int> v = {1, 2, 2, 1};
//    std::cout << is_palindrome(v.begin(), v.end(), [](int a, int b) { return a == b; });

//    std::list<int> l = {1, 2, 3, 4, 5, 6};
//    std::vector<char> v = {'a', 'b', 'c', 'd'};
//    for(auto value : zip(l, v)) {
//        std::cout << value.first << " " << value.second << std::endl;
//    }
}




